const sumar = (num1,num2) => {
    return `La suma es:  ${num1+num2}`
}
const res = sumar(5,3);
console.log(res)

const resta = (num1,num2) => (
    `La resta es:  ${num1-num2}`
    )
const res2 = resta(5,3);
console.log(res2)